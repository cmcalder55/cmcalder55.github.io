
import links from "../../../components/linkMetadata";
import {RepoLink} from "../../../components/Buttons";

const netGraph = () => {
    return (
        <div>
          <h1>Network Graphs</h1>
          <RepoLink link={links.netGraph.url} />
          <p>Placeholder</p>
        </div>
      );
    };
    
export default netGraph;